# AI Travel Planner Web

Next.js frontend for the AI Travel Planner app.

## Getting Started

```bash
npm run dev
```

The frontend reads the API URL from `.env.local`:

```bash
NEXT_PUBLIC_API_BASE_URL=http://localhost:5122
```

Open `http://localhost:3000` after starting the development server.
